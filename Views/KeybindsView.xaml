<UserControl x:Class="KeypadSoftware.Views.KeybindsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KeypadSoftware.Views"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Row 0 -->
        <TextBlock Grid.Row="0" Foreground="White" FontSize="26" Margin="20, 20">Keybinds</TextBlock>

        <!-- Row 1 -->
        <Grid Grid.Row="1" HorizontalAlignment="Left" Width="auto" Margin="20, 0, 0, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto" MinWidth="83"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <!-- Left Button -->
            <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" FontSize="16" Margin="0,0,10,0">Left Button</TextBlock>
            <Button x:Name="EditLeftKeybind" Content="Z"
                    Grid.Row="0" Grid.Column="2" Margin="5"/>
            <Button x:Name="EditLeftKeybindCover" Content="..." IsEnabled="False" Visibility="{Binding EditLeftKeybindCoverVisible}"
                    Grid.Row="0" Grid.Column="2" Margin="5" Panel.ZIndex="1"/>

            <!-- Right Button -->
            <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" FontSize="16" Margin="0,0,10,0">Right Button</TextBlock>
            <Button x:Name="EditRightKeybind" Content="X"
                    Grid.Row="1" Grid.Column="2" Margin="5"/>
            <Button x:Name="EditRightKeybindCover" Content="..." IsEnabled="False" Visibility="{Binding EditRightKeybindCoverVisible}"
                    Grid.Row="1" Grid.Column="2" Margin="5" Panel.ZIndex="1"/>

            <!-- Side Button -->
            <TextBlock Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" FontSize="16" Margin="0,0,10,0">Side Button</TextBlock>
            <Button x:Name="EditSideKeybind" Content="C"
                    Grid.Row="2" Grid.Column="2" Margin="5"/>
            <Button x:Name="EditSideKeybindCover" Content="..." IsEnabled="False" Visibility="{Binding EditSideKeybindCoverVisible}"
                    Grid.Row="2" Grid.Column="2" Margin="5" Panel.ZIndex="1"/>

        </Grid>
    </Grid>
</UserControl>
